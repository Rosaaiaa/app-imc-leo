<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@color/fundoTI">

    <!-- TÍTULO -->
    <TextView
        android:id="@+id/textTitulo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Junção dos App'S"
        android:textColor="@color/textoTI"
        android:textSize="32sp"
        android:textStyle="bold"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/profile_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"/>

    <!-- IMAGEM CIRCULAR -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile_image"
        android:layout_width="160dp"
        android:layout_height="155dp"
        android:src="@drawable/garfield"
        app:civ_border_width="4dp"
        app:civ_border_color="@color/textoTI"
        app:layout_constraintTop_toBottomOf="@id/textTitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- SUBTÍTULO -->
    <TextView
        android:id="@+id/subtitulo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Bem-vindo! Escolha o seu app:"
        android:textColor="@color/textoTI"
        android:textSize="18sp"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/profile_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp" />

    <!-- BOTÃO 1 -->
    <Button
        android:id="@+id/btnGoToSecond"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Tela de Aprov. Aluno"
        android:backgroundTint="@color/botaoTI"
        android:textColor="@color/botaotextoTI"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/subtitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- BOTÃO 2 -->
    <Button
        android:id="@+id/btnGoToThird"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Tela de Agenda e IMC"
        android:backgroundTint="@color/botaoTI"
        android:textColor="@color/botaotextoTI"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnGoToSecond"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
