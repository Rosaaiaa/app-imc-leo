<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BancoDeDados"
        tools:targetApi="31">

        <!-- Agora PaginaInicial é a tela principal -->
        <activity
            android:name=".PaginaInicial"
            android:exported="true"> <!-- Precisa estar como true -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AtualizarUsuario" android:exported="false" />
        <activity android:name=".CadastroUsuarios" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".AprovacaoAlunoActivity" android:exported="false" />
    </application>

</manifest>
